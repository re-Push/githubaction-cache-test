{
  "name": "s3-cache-test",
  "version": "1.0.0",
  "description": "GitHub Actions S3 Cache 테스트 프로젝트",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && echo 'console.log(\"Hello from build!\");' > dist/index.js",
    "start": "node dist/index.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "github-actions",
    "s3",
    "cache",
    "buildx"
  ],
  "author": "",
  "license": "MIT"
} 